se ejcuta el archivo main.py dentro de la carpeta src
Instalar las librerías desde el archivo 
utilizando python -m pip install -r requirements.txt

para generar archivo requirements.txt 
python -m pip freeze > requirements.txt